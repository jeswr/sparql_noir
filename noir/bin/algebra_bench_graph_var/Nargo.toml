[package]
name = "algebra_bench_graph_var"
type = "bin"
authors = [""]
# Readable-surface counterpart of `bench/monolithic_graph_var/`.
# Bench-row in `bench/readable-vs-monolithic.md`.
#
# Query (variable-graph form):
#   SELECT ?s ?g WHERE { GRAPH ?g { ?s <ex:p> ?o } }

[dependencies]
types = { path = "../../lib/types" }
consts = { path = "../../lib/consts" }
utils = { path = "../../lib/utils" }
algebra = { path = "../../lib/algebra" }
