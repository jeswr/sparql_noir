[package]
name = "algebra_bench_filter_comparison"
type = "bin"
authors = [""]
# Readable-surface counterpart of
# `bench/monolithic_filter_comparison/`. Bench-row in
# `bench/readable-vs-monolithic.md`.
#
# Query (a representative mixed FILTER):
#   FILTER (?a != ?b && ?c > ?d) -- one hash-domain, one i64-domain.

[dependencies]
types = { path = "../../lib/types" }
consts = { path = "../../lib/consts" }
utils = { path = "../../lib/utils" }
algebra = { path = "../../lib/algebra" }
