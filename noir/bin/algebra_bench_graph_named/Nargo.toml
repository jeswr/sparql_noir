[package]
name = "algebra_bench_graph_named"
type = "bin"
authors = [""]
# Readable-surface counterpart of `bench/monolithic_graph_named/`.
# Bench-row in `bench/readable-vs-monolithic.md`.
#
# Query (named-graph form):
#   SELECT ?s WHERE { GRAPH <http://example.org/g1> { ?s <ex:p> ?o } }

[dependencies]
types = { path = "../../lib/types" }
consts = { path = "../../lib/consts" }
utils = { path = "../../lib/utils" }
algebra = { path = "../../lib/algebra" }
