[package]
name = "algebra_bench_literal_value"
type = "bin"
authors = [""]
# Readable-surface counterpart of
# `bench/monolithic_literal_value/`. Bench-row in
# `bench/readable-vs-monolithic.md`.
#
# Query:
#   SELECT ?s WHERE { ?s <http://example.org/label> "hello" }

[dependencies]
types = { path = "../../lib/types" }
consts = { path = "../../lib/consts" }
utils = { path = "../../lib/utils" }
algebra = { path = "../../lib/algebra" }
