// Generated by sparql_noir transform
use dep::consts;
use dep::utils;
use dep::types::Triple;

pub(crate) type BGP = [Triple; 1];
pub(crate) struct Variables {
  pub(crate) s: Field,
}

pub(crate) fn checkBinding(bgp: BGP, variables: Variables) {
  assert(variables.s == bgp[0].terms[0].hash);
  assert(bgp[0].terms[2].hash == bgp[0].terms[2].hash);
  assert(consts::hash2([0, consts::encode_string("http://example.org/label")]) == bgp[0].terms[1].hash);
  assert({ utils::bind_term_bytes_plain_string_literal(bgp[0].terms[2], utils::empty_string_lexical_hash(), utils::xsd_string_datatype_hash()); let prefix: [u8; 3] = [0x66, 0x6f, 0x6f]; utils::string_starts_with::<3>(bgp[0].terms[2], prefix, 3) });
}
