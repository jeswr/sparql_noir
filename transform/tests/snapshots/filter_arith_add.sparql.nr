// Generated by sparql_noir transform
use dep::consts;
use dep::utils;
use dep::types::Triple;
use dep::xpath;

pub(crate) type BGP = [Triple; 2];
pub(crate) struct Variables {
  pub(crate) s: Field,
}

pub(crate) type Hidden = [Field; 3];
pub(crate) fn checkBinding(bgp: BGP, variables: Variables, hidden: Hidden) {
  assert(variables.s == bgp[0].terms[0]);
  assert(consts::hash2([0, consts::encode_string("http://example.org/a")]) == bgp[0].terms[1]);
  assert(variables.s == bgp[1].terms[0]);
  assert(consts::hash2([0, consts::encode_string("http://example.org/b")]) == bgp[1].terms[1]);
  assert((((xpath::numeric_add_int((hidden[0]) as i64, (hidden[1]) as i64)) as Field) as i64) > (hidden[2] as i64));
}
