// Generated by sparql_noir transform
use dep::consts;
use dep::utils;
use dep::types::Triple;

pub(crate) type BGP = [Triple; 4];
pub(crate) struct Variables {
  pub(crate) s: Field,
  pub(crate) o: Field,
}

pub(crate) fn checkBinding(bgp: BGP, variables: Variables) {
  let branch_0 = (variables.s == variables.o);
  let branch_1 = (variables.s == bgp[0].terms[0]) & (variables.o == bgp[0].terms[2]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[0].terms[1]);
  let branch_2 = (variables.s == bgp[0].terms[0]) & (bgp[0].terms[2] == bgp[1].terms[0]) & (variables.o == bgp[1].terms[2]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[0].terms[1]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[1].terms[1]);
  let branch_3 = (variables.s == bgp[0].terms[0]) & (bgp[0].terms[2] == bgp[1].terms[0]) & (bgp[1].terms[2] == bgp[2].terms[0]) & (variables.o == bgp[2].terms[2]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[0].terms[1]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[1].terms[1]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[2].terms[1]);
  let branch_4 = (variables.s == bgp[0].terms[0]) & (bgp[0].terms[2] == bgp[1].terms[0]) & (bgp[1].terms[2] == bgp[2].terms[0]) & (bgp[2].terms[2] == bgp[3].terms[0]) & (variables.o == bgp[3].terms[2]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[0].terms[1]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[1].terms[1]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[2].terms[1]) & (consts::hash2([0, consts::encode_string("http://example.org/knows")]) == bgp[3].terms[1]);
  assert(branch_0 | branch_1 | branch_2 | branch_3 | branch_4);
}
