[package]
name = "monolithic_filter_and_or"
type = "bin"
authors = [""]
# Verbatim re-host of `transform/tests/snapshots/filter_and_or.*`
# for side-by-side gate-count comparison against the readable
# rewrite at `noir/bin/algebra_example`. The two share the public-
# input contract so a single Prover.toml can drive both.

[dependencies]
consts = { path = "../../noir/lib/consts" }
types = { path = "../../noir/lib/types" }
utils = { path = "../../noir/lib/utils" }
