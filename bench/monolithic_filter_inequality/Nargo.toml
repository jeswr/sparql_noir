[package]
name = "monolithic_filter_inequality"
type = "bin"
authors = [""]
# Verbatim re-host of `transform/tests/snapshots/filter_inequality.*`.

[dependencies]
consts = { path = "../../noir/lib/consts" }
types = { path = "../../noir/lib/types" }
utils = { path = "../../noir/lib/utils" }
